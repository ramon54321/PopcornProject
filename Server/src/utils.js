/**
 * @module Utils
 */

 /**
 * The function that doubles the input given to it. Or so I think.
 * @param {number} x The parameter to double.
 * @return {number} Double the given parameter.
 */
export function getDouble(x) {
	return 2 * x
}
